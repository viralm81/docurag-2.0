# Core dependencies
streamlit==1.29.0

# FAISS (CPU version) + compatible NumPy
numpy==1.26.0
faiss-cpu==1.7.4

# DuckDuckGo search (latest compatible)
duckduckgo-search==6.2.10

# Other dependencies (add your app-specific ones here)
pandas==2.1.0
requests==2.32.0
python-dotenv==1.1.1

# Optional: text processing / embeddings
huggingface-hub==0.16.4
sentence-transformers==2.2.2
torch==2.3.1
transformers==4.42.0

# Voice / TTS if used
gtts==2.3.2
